<div>
  <div>
    <div>
      <img src="<%= @game.image_url %>">
      <p>INSERT VOTES HERE</p>
    </div>
    <div>
      <p><%= @game.name %></p>
      <p><%= @game.player_range %></p>
      <p><%= @game.play_time %></p>
      <ul>
        <h3><b>Categories:</b></h3>
        <% @game.categories.each do |category| %>
        <li><%= category.name %></li>
        <% end %>
      </ul>
      <ul>
        <h3><b>Mechanics:</b></h3>
        <% @game.tags.each do |tag| %>
        <li><%= tag.name %></li>
        <% end %>
      </ul>
    </div>
  </div>
  <div>
    <p><%= @game.description %></p>
  </div>
  <div>
    <ul>
      <h3><b>Comments:</b></h3>
      <% @game.comments.each do |comment| %>
        <li><%= comment.text %></li>
      <% end %>
    </ul>
  </div>
</div>